{{- if .Values.openshift.enabled -}}
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "cqg.fullname" . }}
spec:
  to:
    kind: Service
    name: {{ include "cqg.fullname" . }}
  tls:
    insecureEdgeTerminationPolicy: Allow
    termination: edge
{{- end }}